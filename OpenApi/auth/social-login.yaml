openapi: 3.0.0
info:
  title: Bidu API Documentation
  version: 1.0.0
  description: API documentation for Bidu e-commerce platform

servers:
  - url: http://localhost:3000
    description: Development server

components:
  schemas:
    SocialLoginRequest:
      type: object
      required:
        - provider
        - token
      properties:
        provider:
          type: string
          enum:
            - google
            - facebook
            - zalo
          description: Social login provider. Use a Google ID token for Google, a Facebook access token for Facebook, and a Zalo authorization code for Zalo.
          example: google
        token:
          type: string
          description: Provider token returned by the client social login SDK. For Zalo, send the authorization code in this field.
          example: eyJhbGciOiJSUzI1NiIsImtpZCI6Ij...
        codeVerifier:
          type: string
          description: Required only for Zalo login. This must match the PKCE code verifier used by the client to get the Zalo authorization code.
          example: zalo-pkce-code-verifier

    SocialLoginResponse:
      type: object
      properties:
        status:
          type: string
          example: success
        code:
          type: integer
          example: 200
        message:
          type: string
          example: Social login successful
        payload:
          type: object
          properties:
            accessToken:
              type: string
              example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
            refreshToken:
              type: string
              example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
            tokenType:
              type: string
              example: Bearer
            expiresIn:
              type: string
              example: 15m

    SocialLoginErrorResponse:
      type: object
      properties:
        status:
          type: string
          example: error
        code:
          type: integer
          example: 400
        message:
          type: string
          example: Validation failed
        errors:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
                example: provider
              message:
                type: string
                example: Provider must be google, facebook, or zalo

paths:
  /auth-buyer/social-login:
    post:
      summary: Login or register with a social provider
      tags:
        - Authentication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SocialLoginRequest'
            examples:
              google:
                summary: Google social login
                value:
                  provider: google
                  token: google-id-token
              facebook:
                summary: Facebook social login
                value:
                  provider: facebook
                  token: facebook-access-token
              zalo:
                summary: Zalo social login
                value:
                  provider: zalo
                  token: zalo-authorization-code
                  codeVerifier: zalo-pkce-code-verifier
      responses:
        '200':
          description: Social login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SocialLoginResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SocialLoginErrorResponse'
        '401':
          description: Invalid provider token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SocialLoginErrorResponse'
              example:
                status: error
                code: 401
                message: Invalid social login token
                errors: []
        '403':
          description: Account inactive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SocialLoginErrorResponse'
              example:
                status: error
                code: 403
                message: Your account has been inactive
                errors: []
        '409':
          description: Social account conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SocialLoginErrorResponse'
              example:
                status: error
                code: 409
                message: Email is already linked to a different social account
                errors: []
        '500':
          description: Social login provider is not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SocialLoginErrorResponse'
              example:
                status: error
                code: 500
                message: Zalo social login is not configured
                errors: []
